import 'route_path.dart';

class RouteNames {
  static const String home = RoutePath.home;
  static const String projects = '${RoutePath.home}${RoutePath.projects}';
  // static const String editTodo = '${RoutePath.todo}${RoutePath.editTodo}';
}
